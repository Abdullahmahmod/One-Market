# One Market - مستند التوثيق الشامل
## متجر الخضار الطازج

**تاريخ آخر تحديث:** 30 يناير 2026  
**الإصدار:** 1.0 (منظف وموثق)

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [هيكل المشروع](#هيكل-المشروع)
3. [وصف الملفات](#وصف-الملفات)
4. [نظام البيانات](#نظام-البيانات)
5. [قائمة الدوال](#قائمة-الدوال)
6. [تحسينات التنظيف](#تحسينات-التنظيف)
7. [التعليمات المستقبلية](#التعليمات-المستقبلية)

---

## 🎯 نظرة عامة

**One Market** متجر إلكتروني لبيع الخضار الطازة بنظام اشتراكات مرنة. يوفر للعملاء:
- 📅 باقات يومية وأسبوعية وشهرية
- ⚙️ خيار تخصيص الباقات حسب الاحتياجات
- 🛒 عربة تسوق ديناميكية
- 💾 حفظ البيانات محلياً (localStorage)

---

## 📁 هيكل المشروع

```
One Market/
├── index.html              # الصفحة الرئيسية (اختيار الباقات)
├── cart.html               # صفحة العربة والدفع
├── shop.html               # صفحة المتجر
├── product.html            # تفاصيل المنتج
├── about.html              # عن المتجر
├── checkout.html           # طرق الدفع
├── orders.html             # سجل الطلبات
├── main.js                 # ملف JavaScript الرئيسي (كل الوظائف)
├── theme.css               # أنماط CSS الرئيسية
├── main.css                # نسخة احتياطية من الأنماط
├── responsive.css          # ملف احتياطي للأنماط الإضافية
├── DOCUMENTATION.md        # هذا الملف
└── images/                 # مجلد الصور (خبز، طماطم، إلخ)
    ├── bread.png
    ├── tomato.png
    ├── tomato slice.png
    ├── filfil.png
    └── vigner.png
```

---

## 📄 وصف الملفات

### **HTML Files** (ملفات HTML)

| الملف | الوصف | الحالة |
|-------|-------|--------|
| `index.html` | الصفحة الرئيسية - عرض الباقات وعربة التسوق | ✅ نظيف |
| `cart.html` | صفحة العربة المنفصلة - عرض الباقة المختارة وبيانات التوصيل | ✅ نظيف |
| `shop.html` | صفحة المتجر (قابلة للتوسع) | ✅ نظيف |
| `product.html` | صفحة تفاصيل المنتج | ✅ نظيف |
| `about.html` | معلومات عن المتجر | ✅ نظيف |
| `checkout.html` | خيارات الدفع | ✅ نظيف |
| `orders.html` | سجل الطلبات | ✅ نظيف |

**المميزات:**
- توحيد الصيغة (meta tags، indentation)
- تعليقات هيكلية واضحة (<!-- ===== Section ===== -->)
- بدون أكواد JavaScript مضمنة

---

### **CSS Files** (ملفات الأنماط)

#### `theme.css` ✅ الملف الأساسي
- **الحجم:** ~920 سطر
- **المتغيرات:** 
  - الألوان الأساسية (primary, secondary, accent)
  - الظلال (shadow-sm, shadow-md, shadow-lg)
- **المقاطع:**
  - `:root` variables
  - Header styling
  - Navigation
  - Package cards
  - Cart sidebar
  - Form styles
  - Responsive design
  - Scrollbar styling

#### `main.css` 📦 نسخة احتياطية
- نسخة من الأنماط الأساسية
- معلمة للرجوع إليها عند الحاجة

#### `responsive.css` 📝 احتياطي
- فارغ حالياً
- مخصص لقواعد إضافية في المستقبل

---

### **JavaScript File** (ملف JavaScript الرئيسي)

#### `main.js` ✅ منظم وموثق
- **الحجم:** ~730 سطر
- **الأقسام الرئيسية:**

##### 1️⃣ **بيانات الباقات والمنتجات** (أسطر 1-120)
```javascript
packageData = {
  daily, half, week, month
}
itemLabels, itemEmojis, itemUnits, defaultPrices
frequencyMultiplier
```

##### 2️⃣ **حالة التطبيق** (Global State) (أسطر 122-130)
```javascript
let currentPackage = null;
```

##### 3️⃣ **وظائف إدارة الباقات** (أسطر 132-340)
- `selectPackage()` - اختيار باقة
- `showCustomPackageForm()` - عرض نموذج التخصيص
- `closeCustomPackage()` - إغلاق النموذج
- `addCustomPackageToCart()` - إضافة باقة مخصصة
- `updateCart()` - تحديث عرض العربة
- `updateFrequency()` - تغيير التكرار
- `removePackage()` - إزالة الباقة
- `toggleCart()` - فتح/إغلاق عربة التسوق

##### 4️⃣ **وظائف صفحة العربة** (أسطر 342-550)
- `loadCartData()` - تحميل البيانات من localStorage
- `renderCart()` - عرض محتويات العربة
- `updateFrequencyDisplay()` - تحديث السعر
- `handleSubmit()` - معالجة إرسال الطلب

##### 5️⃣ **معالج الصفحة الرئيسية** (أسطر 552-630)
- `setupIndexPageForm()` - إعداد نموذج الصفحة الرئيسية

##### 6️⃣ **التهيئة عند التحميل** (أسطر 632-650)
- `DOMContentLoaded` event listener
- تهيئة مناسبة لكل صفحة

---

## 💾 نظام البيانات

### **packageData Object**
```javascript
{
  daily: {
    name: 'يومية',
    emoji: '📅',
    frequency: 'يومي',
    price: 30,
    basePrice: 30,
    items: { tomato: 0.5, onion: 0.3, ... }
  },
  // ... باقي الباقات
}
```

### **localStorage Keys**
- `cartPackage` - تخزين الباقة المختارة

### **Item Structure**
```javascript
{
  tomato: quantity,    // كجم
  onion: quantity,     // كجم
  cucumber: quantity,  // كجم
  chili: quantity,     // وحدة
  potato: quantity     // كجم
}
```

---

## 🔧 قائمة الدوال

### **وظائف عامة**
| الدالة | المعاملات | الوصف |
|--------|----------|-------|
| `selectPackage(pkgType)` | string | اختيار باقة والانتقال للعربة |
| `toggleCart()` | none | فتح/إغلاق عربة التسوق |
| `openCart()` | none | فتح عربة التسوق |

### **وظائف الباقات المخصصة**
| الدالة | المعاملات | الوصف |
|--------|----------|-------|
| `showCustomPackageForm()` | none | عرض نموذج التخصيص |
| `closeCustomPackage()` | none | إغلاق النموذج |
| `addCustomPackageToCart()` | none | إضافة الباقة المخصصة |

### **وظائف العربة**
| الدالة | المعاملات | الوصف |
|--------|----------|-------|
| `updateCart()` | none | تحديث عرض العربة |
| `updateFrequency(freq)` | string | تحديث التكرار والسعر |
| `removePackage()` | none | إزالة الباقة من العربة |

### **وظائف صفحة العربة**
| الدالة | المعاملات | الوصف |
|--------|----------|-------|
| `loadCartData()` | none | تحميل البيانات من localStorage |
| `renderCart()` | none | عرض محتويات العربة |
| `updateFrequencyDisplay(freq)` | string | تحديث عرض السعر |
| `handleSubmit(e)` | Event | معالجة إرسال الطلب |

---

## 🎨 تحسينات التنظيف

### **مرحلة 1: تنسيق CSS** ✅
- ✅ تصحيح خطأ نحوي في `.site-nav a` (t + text-decoration)
- ✅ إضافة تعليقات توضيحية لكل قسم
- ✅ توثيق متغيرات الألوان والظلال

### **مرحلة 2: استخراج JavaScript** ✅
- ✅ استخراج جميع الأكواد من HTML إلى `main.js`
- ✅ تنظيم الكود بأقسام واضحة
- ✅ إضافة JSDoc comments لكل دالة
- ✅ إزالة المتغيرات المكررة

### **مرحلة 3: تنظيف HTML** ✅
- ✅ توحيد meta tags (إزالة `/` الإضافية)
- ✅ توحيد indentation (tabs → 2 spaces)
- ✅ إضافة تعليقات هيكلية واضحة
- ✅ إزالة عناصر غير مستخدمة (hidden inputs)
- ✅ استبدال جميع السكريبتات المضمنة بـ `<script src="main.js"></script>`

### **النتائج:**
- 📊 تقليل حجم ملفات HTML بـ ~50%
- 🎯 تحسين قابلية الصيانة
- 🔍 سهولة البحث عن الأكواد
- ⚡ أداء أفضل (ملف واحد بدل تكرار)

---

## 🚀 التعليمات المستقبلية

### **الأولويات العالية:**
1. **إضافة قاعدة بيانات** (Firebase أو MongoDB)
   - حفظ الطلبات بشكل دائم
   - إدارة حسابات العملاء

2. **نظام الدفع**
   - Stripe أو Fawry integration
   - تأكيد الدفع

3. **واجهة إدارية**
   - عرض الطلبات الجديدة
   - إدارة الباقات والأسعار

### **الأولويات المتوسطة:**
4. **تحسين الأداء**
   - Minify CSS و JavaScript
   - Lazy loading للصور
   - Service Worker للـ offline mode

5. **تحسينات UI/UX**
   - Animations أفضل
   - Dark mode
   - PWA support

6. **محرك البحث والفلاتر**
   - البحث عن المنتجات
   - فلترة حسب الفئة

---

## 📞 نقاط التواصل والمتغيرات

### **Event Listeners:**
- `DOMContentLoaded` - تهيئة الصفحة
- `click` - اختيار الباقة، فتح/إغلاق العربة
- `change` - تغيير التكرار
- `submit` - إرسال النموذج

### **API Integrations:**
- SweetAlert2 - رسائل التنبيه
- Google Fonts (Cairo) - الخط
- localStorage - تخزين محلي

---

## ✅ قائمة التحقق

- [x] تنظيف ملفات CSS
- [x] استخراج JavaScript
- [x] تنظيف ملفات HTML
- [x] توحيد الصيغة والتنسيق
- [x] إضافة تعليقات واضحة
- [x] التحقق من عدم وجود أخطاء
- [x] توثيق شامل

---

## 📝 ملاحظات المطور

### **تحديثات 2026-01-30:**
1. تصحيح خطأ نحوي في CSS
2. استخراج ~750 سطر من JavaScript
3. توحيد 7 ملفات HTML
4. إضافة توثيق شامل

### **حالة الملفات:**
- ✅ **main.js:** منظم وموثق بالكامل
- ✅ **theme.css:** مصحح وجاهز
- ✅ **جميع ملفات HTML:** نظيفة وموحدة
- ✅ **بدون أخطاء:** تم التحقق من الكود

---

## 🎓 للمبتدئين

### **كيفية فهم الكود:**
1. ابدأ بـ `main.js` (أسطر 1-120): فهم البيانات
2. ثم انتقل للدوال (أسطر 132-340): كيف تعمل الباقات
3. ثم ادرس صفحة العربة (أسطر 342-550): تفاصيل الشراء
4. أخيراً ادرس HTML: الهيكل والعناصر

### **أماكن الإضافات المستقبلية:**
- **نموذج جديد؟** أضفه بعد السطر 550 في `main.js`
- **دالة جديدة؟** أضفها في القسم المناسب
- **نمط جديد؟** استخدم `theme.css`

---

**آخر تحديث:** 30 يناير 2026  
**الحالة:** ✅ جاهز للاستخدام
