# 💰 دليل سريع: تغيير الأسعار

## 🎯 النتيجة النهائية

**الأسعار الآن تُحسب تلقائياً من مجموع أسعار المنتجات!**

عندما تغيّر سعر منتج، **جميع الباقات التي تحتويه تُحدّث سعرها تلقائياً** ✅

---

## 🚀 كيفية تغيير الأسعار

### الطريقة: افتح `js/config.js` وغيّر الأسعار

```javascript
const PRODUCTS = {
  tomato: {
    label: '🍅 طماطم',
    unitPrice: 15  // ← غيّر الرقم هنا
  },
  onion: {
    label: '🧅 بصل',
    unitPrice: 12  // ← أو هنا
  },
  cucumber: {
    label: '🥒 خيار',
    unitPrice: 18  // ← أو هنا
  },
  chili: {
    label: '🌶️ شطة',
    unitPrice: 8   // ← أو هنا
  },
  potato: {
    label: '🥔 بطاطس',
    unitPrice: 6   // ← أو هنا
  }
};
```

**ثم:**
1. احفظ الملف (Ctrl+S)
2. أعد تحميل الصفحة (F5 أو Ctrl+R)
3. ✅ الأسعار تحدثت تلقائياً!

---

## 📊 أمثلة عملية

### مثال 1: رفع سعر الطماطم

```javascript
PRODUCTS.tomato.unitPrice = 20  // ← من 15 إلى 20
```

**النتيجة:**
```
الباقة الأسبوعية (قبل): 184 جنيه
الباقة الأسبوعية (بعد):  209 جنيه ✅
الفرق: 5 كجم × 5 جنيه = 25 جنيه
```

### مثال 2: خفض أسعار كل المنتجات

```javascript
PRODUCTS.tomato.unitPrice = 12    // ← من 15 إلى 12
PRODUCTS.onion.unitPrice = 10     // ← من 12 إلى 10
PRODUCTS.cucumber.unitPrice = 15  // ← من 18 إلى 15
PRODUCTS.chili.unitPrice = 6      // ← من 8 إلى 6
PRODUCTS.potato.unitPrice = 5     // ← من 6 إلى 5
```

**النتيجة:**
```
جميع الباقات تُحدّث سعرها تلقائياً ✅
```

### مثال 3: عرض خاص (20% أقل)

```javascript
PRODUCTS.tomato.unitPrice = Math.round(15 * 0.8)    // 12
PRODUCTS.onion.unitPrice = Math.round(12 * 0.8)     // 10
PRODUCTS.cucumber.unitPrice = Math.round(18 * 0.8)  // 14
PRODUCTS.chili.unitPrice = Math.round(8 * 0.8)      // 6
PRODUCTS.potato.unitPrice = Math.round(6 * 0.8)     // 5
```

---

## ✅ اختبر الأسعار

### في صفحة الويب:
1. افتح المتصفح: `http://localhost:8000`
2. انظر إلى أسعار الباقات في الصفحة الرئيسية ✅

### في Console (F12):
```javascript
// اختبر الأسعار
calculatePackagePrice(PACKAGES.week)   // أسبوعية
calculatePackagePrice(PACKAGES.half)   // نصف أسبوعية
calculatePackagePrice(PACKAGES.month)  // شهرية
```

### شغّل كل الاختبارات:
1. افتح `PRICE_TESTS.js` و انسخ المحتوى
2. في Console (F12)، اكتب: `runAllPriceTests()`
3. شاهد النتائج ✅

---

## 📈 جدول الحسابات الحالية

| الباقة | المكونات | الحساب | السعر |
|-------|---------|--------|-------|
| 🍅 يومية | 0.5 طماطم، 0.3 بصل، 0.2 خيار، 0.1 شطة، 0.5 بطاطس | (0.5×15) + (0.3×12) + (0.2×18) + (0.1×8) + (0.5×6) | 15 جنيه |
| 🔄 نصف أسبوعية | 2.5 طماطم، 1.5 بصل، 0.75 خيار، 1 شطة، 2.5 بطاطس | (2.5×15) + (1.5×12) + (0.75×18) + (1×8) + (2.5×6) | 93 جنيه |
| 📦 أسبوعية | 5 طماطم، 3 بصل، 1.5 خيار، 2 شطة، 5 بطاطس | (5×15) + (3×12) + (1.5×18) + (2×8) + (5×6) | 184 جنيه |
| 📅 شهرية | 20 طماطم، 12 بصل، 6 خيار، 8 شطة، 20 بطاطس | (20×15) + (12×12) + (6×18) + (8×8) + (20×6) | 734 جنيه |

---

## 🎓 الفرق بين القديم والجديد

| الحالة | القديم | الجديد |
|--------|--------|--------|
| **عند تغيير سعر الطماطم من 15 إلى 20** | ❌ أسعار الباقات ثابتة (186 جنيه) | ✅ أسعار الباقات تتحدّث (209 جنيه) |
| **عند إضافة منتج جديد** | ❌ يجب حساب السعر يدوياً | ✅ يتحسب تلقائياً |
| **عند تغيير كمية منتج في الباقة** | ❌ يجب تحديث السعر يدوياً | ✅ يتحسب تلقائياً |
| **المرونة** | ❌ صعبة | ✅ سهلة جداً |

---

## 💡 نصائح إضافية

### استخدم الدالة المساعدة:
```javascript
// بدل ما تحسب بنفسك
const price = calculatePackagePrice(PACKAGES.week);
// يعيدك النتيجة المحسوبة تلقائياً ✅
```

### للمزيد من المعلومات:
- اقرأ: `docs/DYNAMIC_PRICING.md`
- اختبر: `PRICE_TESTS.js`

---

## ❓ أسئلة شائعة

**س: هل يجب أغيّر أي شيء تاني؟**
الجواب: لا، فقط غيّر الأسعار في `js/config.js` ✅

**س: هل الباقات تتحدّث على الفور؟**
الجواب: بعد حفظ الملف وتحديث الصفحة ✅

**س: ماذا لو نسيت تحديث الصفحة؟**
الجواب: اضغط F5 أو Ctrl+R ✅

**س: هل يعمل على الهاتف؟**
الجواب: نعم! جميع الأجهزة متعاملة ✅

---

## 🎉 انتهينا!

الأسعار الآن **ديناميكية وتحدّث نفسها تلقائياً!** 🚀

جرّب الآن:
1. غيّر سعر منتج
2. احفظ الملف
3. أعد تحميل الصفحة
4. شوف الأسعار تحدثت! ✅
