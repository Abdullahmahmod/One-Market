# 💰 تحديث الأسعار الديناميكية

## ✅ التغييرات التي تمت

الآن الأسعار تُحسب **ديناميكياً** من مجموع أسعار المكونات مباشرة!

### ماذا تغيّر؟

**قبل:**
```javascript
const PACKAGES = {
  week: {
    basePrice: 186,  // سعر ثابت
    items: { tomato: 5, onion: 3, ... }
  }
};
```

**الآن:**
```javascript
const PACKAGES = {
  week: {
    // لا يوجد basePrice - السعر يُحسب تلقائياً
    items: { tomato: 5, onion: 3, ... }
  }
};
// السعر = (5 كجم طماطم × 15 جنيه) + (3 كجم بصل × 12 جنيه) + ...
```

---

## 🎯 الآن عندما تغيّر سعر منتج، الباقة تتحدث تلقائياً!

### مثال عملي:

1. **قبل التغيير:**
   - سعر الطماطم: 15 جنيه/كجم
   - الباقة الأسبوعية (5 كجم طماطم): السعر المحسوب ديناميكياً

2. **غيّر سعر الطماطم:**
   في `js/config.js`:
   ```javascript
   const PRODUCTS = {
     tomato: {
       label: '🍅 طماطم',
       unitPrice: 20  // ← غيّر من 15 إلى 20
     }
   };
   ```

3. **بعد التغيير:**
   - الباقة الأسبوعية تُحدّث السعر **تلقائياً**!
   - كل الباقات التي تحتوي على الطماطم تُحدّث سعرها

---

## 🔧 كيفية تحديث الأسعار

### 1. غيّر سعر منتج واحد

في `js/config.js`:
```javascript
const PRODUCTS = {
  tomato: {
    label: '🍅 طماطم',
    emoji: '🍅',
    name: 'طماطم',
    unit: 'كجم',
    unitPrice: 20  // ← غيّر السعر هنا
  },
  onion: {
    label: '🧅 بصل',
    emoji: '🧅',
    name: 'بصل',
    unit: 'كجم',
    unitPrice: 15  // ← أو هنا
  },
  cucumber: {
    label: '🥒 خيار',
    emoji: '🥒',
    name: 'خيار',
    unit: 'كجم',
    unitPrice: 20  // ← أو هنا
  },
  chili: {
    label: '🌶️ شطة',
    emoji: '🌶️',
    name: 'شطة',
    unit: 'وحدة',
    unitPrice: 10  // ← أو هنا
  },
  potato: {
    label: '🥔 بطاطس',
    emoji: '🥔',
    name: 'بطاطس',
    unit: 'كجم',
    unitPrice: 8   // ← أو هنا
  }
};
```

**ثم:**
- احفظ الملف
- أعد تحميل الصفحة
- **جميع الباقات تُحدّث سعرها تلقائياً!** ✅

### 2. أمثلة على الحسابات

**الباقة الأسبوعية الحالية:**
```
🍅 طماطم:  5 كجم × 15 جنيه = 75 جنيه
🧅 بصل:    3 كجم × 12 جنيه = 36 جنيه
🥒 خيار:   1.5 كجم × 18 جنيه = 27 جنيه
🌶️ شطة:   2 وحدة × 8 جنيه = 16 جنيه
🥔 بطاطس: 5 كجم × 6 جنيه = 30 جنيه
                    ────────────
                    المجموع = 184 جنيه
```

**إذا غيّرت سعر الطماطم إلى 20 جنيه:**
```
🍅 طماطم:  5 كجم × 20 جنيه = 100 جنيه  ← تغيّر!
🧅 بصل:    3 كجم × 12 جنيه = 36 جنيه
🥒 خيار:   1.5 كجم × 18 جنيه = 27 جنيه
🌶️ شطة:   2 وحدة × 8 جنيه = 16 جنيه
🥔 بطاطس: 5 كجم × 6 جنيه = 30 جنيه
                    ────────────
                    المجموع = 209 جنيه  ← تحدّث تلقائياً!
```

---

## 📱 الأماكن التي يتحدث فيها السعر

1. **الصفحة الرئيسية (index.html)**
   ✅ عند تحميل الصفحة، الأسعار تُحسب من المنتجات

2. **صفحة اختيار الباقة**
   ✅ عند الضغط على "اختر"، السعر يُحسب

3. **صفحة العربة (cart.html)**
   ✅ عند تغيير التكرار، السعر يُحسب

4. **الطلبات المخصصة**
   ✅ الأسعار تُحسب من اختيار العميل

---

## 🎓 الفرق بين النسخة القديمة والجديدة

| النسخة | السعر | المشكلة | الحل |
|--------|-------|---------|------|
| **القديمة** | 186 جنيه (ثابت) | عند تغيير سعر منتج، الباقة لا تتحدث | لا يوجد |
| **الجديدة** | ديناميكي = مجموع الأسعار | تُحسب من المنتجات | ✅ يتحدث تلقائياً |

---

## 🔍 كيف يعمل الكود

**الدالة الرئيسية:**
```javascript
function calculatePackagePrice(pkg, items = null) {
  const itemsToCalculate = items || pkg.items;
  let totalPrice = 0;

  // حلقة على كل منتج في الباقة
  Object.entries(itemsToCalculate).forEach(([itemId, quantity]) => {
    if (PRODUCTS[itemId]) {
      const unitPrice = PRODUCTS[itemId].unitPrice;  // ← السعر الحالي
      totalPrice += quantity * unitPrice;  // ← اضرب الكمية × السعر
    }
  });

  return Math.round(totalPrice);  // ← أرجع النتيجة المقربة
}
```

---

## ✨ الفوائد

✅ **سهل التحديث**
- تغيّر السعر في مكان واحد فقط
- جميع الباقات تُحدّث نفسها

✅ **لا مزيد من الأخطاء**
- لا تنسى تحديث سعر الباقة
- السعر دائماً صحيح

✅ **مرن للمستقبل**
- إذا أضفت منتج جديد، يُحسب تلقائياً
- إذا غيّرت كمية المنتج في الباقة، السعر يتحدث

✅ **جاهز للـ Admin Dashboard**
- عند بناء لوحة التحكم، السعر يُحسب ديناميكياً
- لا حاجة لإعادة حساب يدوية

---

## 🚀 الاستخدام الفعلي

### سيناريو 1: تغيير سعر يومي
```javascript
// يوم الاثنين: أسعار عادية
PRODUCTS.tomato.unitPrice = 15;

// يوم الثلاثاء: الطماطم رخيصة
PRODUCTS.tomato.unitPrice = 10;
// ✅ جميع الباقات تُحدّث السعر تلقائياً!

// يوم الأربعاء: الطماطم غالية
PRODUCTS.tomato.unitPrice = 20;
// ✅ جميع الباقات تُحدّث السعر تلقائياً!
```

### سيناريو 2: عرض خاص
```javascript
// عرض: كل المنتجات بـ 20% أقل
PRODUCTS.tomato.unitPrice = 15 * 0.8;    // 12
PRODUCTS.onion.unitPrice = 12 * 0.8;     // 9.6 → 10
PRODUCTS.cucumber.unitPrice = 18 * 0.8;  // 14.4 → 14
// ✅ جميع الباقات تُحدّث السعر تلقائياً!
```

---

## 💡 نصائح

1. **دائماً احفظ الملف بعد التغيير**
   - Ctrl+S (Windows/Linux)
   - Cmd+S (Mac)

2. **أعد تحميل الصفحة**
   - Ctrl+R أو F5 (Windows/Linux)
   - Cmd+R أو Cmd+Shift+R (Mac)

3. **تحقق من المتصفح**
   - افتح DevTools (F12)
   - اذهب إلى Console
   - تحقق من عدم وجود أخطاء

4. **اختبر الدالة**
   - في Console، اكتب:
   ```javascript
   calculatePackagePrice(PACKAGES.week)
   // يجب أن تُرجع السعر الجديد
   ```

---

**✅ تم! الأسعار الآن ديناميكية وتُحدّث نفسها تلقائياً!**
